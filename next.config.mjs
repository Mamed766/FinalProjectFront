/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "darkfashion.wpengine.com",
      "http://localhost:3001/",
      "secure.gravatar.com",
      "localhost",
    ],
  },
};

export default nextConfig;
