/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "darkfashion.wpengine.com",
      "http://localhost:3001/",
      "localhost",
    ],
  },
};

export default nextConfig;
